package util;

public class IsEqualUtil {

    public <T> boolean isEqual(T itemOne, T itemTwo) {
        // TODO: Verify if need it, because already have this method created in Java class Object (it's similar, but haven't this method with Int values)
        return itemOne == itemTwo;
    }

}
